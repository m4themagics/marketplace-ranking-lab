# Reports

Результатов пока нет.

Один отчёт — на один эксперимент из `experiments/`. Отчёт появляется только после того, как
пройден publication gate из [research protocol](../docs/research-protocol.md): тесты
корректности зелёные, протокол не менялся молча после просмотра числа, посчитана
неопределённость, разобраны ручные примеры.

Рабочая таблица, полученная прогоном, — это ещё не результат. Результат — это число с
интервалом, у которого записаны команда, seed, commit и fingerprint входных данных.

Каждый отчёт содержит раздел «что не сработало». Он читается сильнее, чем список побед, и
пишется в том же прогоне, а не задним числом.
